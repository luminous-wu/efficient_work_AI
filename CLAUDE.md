# 项目规则 · 链接脚本 / 内存布局分析工作区

这是个人学习与分析工作区（汽车电子区域控制器 / OS 链接脚本方向）。任何 AI 助手在此工作区内须遵守：

1. **禁止臆造内存数值**：任何地址/大小/对齐/段属性，若未由用户提供，一律用 `<<CONFIRM:来源>>` 占位，严禁猜测填值。
2. **禁止生成可量产完整链接脚本**：只产出带注释的最小片段 + 验证步骤；完整脚本必须经真实厂商工具链验证。
3. **结论可验证**：任何内存数值类结论须附"如何用 .map / readelf / objdump / 数据手册 独立验证"。
4. **保密卫生**：`inputs/`、`*.map`、`*.elf` 视为脱敏样本；不得据此推断真实客户/项目/芯片；不输出还原推测。
5. **安全不外包**：功能安全 / ASIL / MPU 内存隔离决策永远由人主导，AI 仅提供 review checklist。
6. **写分析脚本时**：纯标准库优先、附 pytest、对畸形 .map 健壮、逻辑可读（用户须能看懂维护）。

> 方法论详见 `docs/01-methodology.md`；场景化用法见 `docs/02-domain-handbook.md`。
