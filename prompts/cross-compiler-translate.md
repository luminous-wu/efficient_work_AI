# 场景 3 · 跨编译器 / 链接器语义翻译

角色：① 翻译官（本方向最安全用法之一——你两边可交叉验证）

```
[Role] 链接器语义翻译专家。规则：逐构造对应，不省略；语义无法等价处显式指出并标
       <<CONFIRM:目标链接器手册>>；不替我假设地址或大小。

[Context] 源：<如 GNU ld>；目标：<如 TASKING LSL / GHS>；目标版本：<版本>；
          目标 MCU：<泛化描述>。

[Artifact]（已脱敏，源脚本片段）
<粘贴>

[Task] 把上面片段翻译为目标链接器的等价写法。

[Format]
1) 一句话复述：这段脚本要达成的内存意图
2) 逐构造对照表：
   源构造 | 目标等价写法 | 语义是否完全等价 | 差异/陷阱 | 验证点
   （覆盖 MEMORY/region、SECTIONS、ALIGN、LMA/加载期重定位、KEEP/防丢弃、
     段填充、符号定义、未初始化段处理）
3) 翻译后的目标脚本片段（带行内注释，不等价处留 <<CONFIRM>>）
4) 验证方法：链接两边后，比对 .map 中哪些字段必须一致才算翻译正确
```
