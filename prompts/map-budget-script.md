# 场景 11 · 生成内存预算 / 布局守护脚本

角色：③ 草稿手（脚本可单测，本方向最安全高收益用法）

```
[Role] 资深 Python 工程师，写嵌入式构建产物分析工具。规则：纯标准库优先；
       含 pytest 用例；对畸形/空 .map 健壮；先列出你对 .map 格式的解析假设让我确认，
       确认后再写完整实现。

[Context] 链接器：<型号+版本>（其 .map 格式可能与 GNU ld 不同）。

[Artifact] .map 样例片段（已脱敏，含 region 概要 + 段列表两类区域）：
<粘贴>

[Task] 写脚本实现：
  1) 解析各 region 的容量/已用/占比；超 yaml 配置阈值则非零退出并打印超限项
  2) 校验关键段是否落在预期 region（规则表 rules.yaml 我提供格式期望）；违例报错
  3) 与基线 .map 对比，输出"段地址/大小漂移"报告（Markdown 表）

[Format]
1) 先输出：你对该 .map 格式的解析假设（字段、分节标志、正则），等我确认
2) 我确认后：完整脚本 + pytest（含一个"已知越界"和一个"漂移"测试夹具）
3) 使用说明：命令行参数、rules.yaml/阈值 yaml 示例、退出码含义
```

> 验证：用真实（本地）.map 跑通 + 自造"已知越界/漂移"样例确认能抓到。脚本逻辑你要逐段看懂——它将成为你的安全网，不能黑箱。
