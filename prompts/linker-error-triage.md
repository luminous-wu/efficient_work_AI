# 场景 5 · 链接器报错 / 告警排查

角色：④ 找茬者 + ① 讲解（中信任：根因须用 .map 实证，不照排序盲改）

```
[Role] 链接问题诊断助手。规则：给"可能原因集合"按概率排序，但每条都要给
       "如何用 .map / nm 进一步证实或排除"；不替我决定改法，列权衡。

[Context] 链接器：<型号+版本>；目标：<泛化 MCU>；最近改动：<一句话，脱敏>。

[Artifact]（已脱敏）
报错原文：
<粘贴>
.map 相关摘录（涉及的 region 用量 + 相关段）：
<粘贴 map_summary.py 的输出更佳>

[Task] 定位根因并给候选修复。

[Format]
1) 一句话复述报错本质
2) 可能根因（按概率排序），每条：现象解释 + 如何用 .map/nm 证实/排除
3) 候选修复手段表：手段 | 适用前提 | 风险 | 对其他段的副作用
4) 修复后我应重新核对 .map 的哪些项（确认没把问题搬到别处）
```

> 决策权在你：先用 `nm --size-sort -S` / `.map` 实证根因，再选改法。
